# Database Configuration
# For Supabase Session Pooler (use this in Vercel):
# Add ?pgbouncer=true&connection_limit=1 to the end of your Supabase pooler URL
# Example: postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-1-ap-south-1.pooler.supabase.com:5432/postgres?pgbouncer=true&connection_limit=1
DATABASE_URL="postgresql://username:password@host:port/database?pgbouncer=true&connection_limit=1"

# For local development (direct connection, remove the parameters):
# DATABASE_URL="postgresql://username:password@localhost:5432/database"

# Next.js Configuration
NODE_ENV="development"

# JWT Secret (generate a random secret for production)
# You can generate one with: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# Admin Credentials (for initial admin login)
ADMIN_EMAIL="admin@boozmandu.com"
ADMIN_PASSWORD_HASH="hash-will-be-generated-on-first-setup"
